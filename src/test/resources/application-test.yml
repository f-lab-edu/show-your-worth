spring:
  config:
    activate:
      on-profile: "test"
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:muus-test;MODE=mysql;
    username: redgem92
    password:
  jpa:
    database: h2
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        use_sql_comments: true
cloud:
  credentials:
    access-key: access-test
    secret-key: secret-test
  storage:
    endpoint: https://kr.object.ncloudstorage.com
    region:
      static: kr-standard
    bucket-name: muus-test
logging:
  level:
    root: info
    org:
      hibernate:
        type: trace
